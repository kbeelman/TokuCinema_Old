@model IEnumerable<TokuCinema.Models.AudioTrack>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AudioTrackType.AudioTrackName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VideoVersion.VideoVersionId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AudioTrackType.AudioTrackName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoVersion.VideoVersionId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AudioTrackid }) |
            @Html.ActionLink("Details", "Details", new { id=item.AudioTrackid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AudioTrackid })
        </td>
    </tr>
}

</table>
