@model IEnumerable<TokuCinema.Models.SubtitleTrack>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SubtitleTrackType.SubtitleTrackName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VideoVersion.VideoVersionId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubtitleTrackType.SubtitleTrackName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoVersion.VideoVersionId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubtitleTrackId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubtitleTrackId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubtitleTrackId })
        </td>
    </tr>
}

</table>
