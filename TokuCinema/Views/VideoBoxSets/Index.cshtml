@model IEnumerable<TokuCinema.Models.VideoBoxSet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VideoBoxSetType.VideoBoxSetTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VideoRelease.CatalogCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VideoBoxSetType.VideoBoxSetTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoRelease.CatalogCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VideoBoxSetId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VideoBoxSetId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VideoBoxSetId })
        </td>
    </tr>
}

</table>
