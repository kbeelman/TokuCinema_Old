@model IEnumerable<TokuCinema.Models.Packaging>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PackagingDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PackagingName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PackagingDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PackagingName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PackagingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PackagingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PackagingId })
        </td>
    </tr>
}

</table>
