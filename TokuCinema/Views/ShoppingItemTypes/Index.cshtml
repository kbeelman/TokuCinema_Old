@model IEnumerable<TokuCinema.Models.ShoppingItemType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShoppingItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PurchaseLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Company.CompanyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShoppingItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PurchaseLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Company.CompanyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoppingItemTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoppingItemTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoppingItemTypeId })
        </td>
    </tr>
}

</table>
