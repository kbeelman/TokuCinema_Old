@model IEnumerable<TokuCinema.Models.VideoBoxSetType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VideoBoxSetTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VideoBoxSetDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VideoBoxSetTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoBoxSetDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VideoBoxSetTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VideoBoxSetTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VideoBoxSetTypeId })
        </td>
    </tr>
}

</table>
